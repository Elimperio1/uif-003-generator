# Samples

This directory will hold anonymised CSVs and an expected `.003` output used
for automated regression testing in later steps.

For local testing with your own real Sage exports, place them in
`samples/private/` — that path is gitignored so real PII never leaves your
machine.
