# Copy this file to `.streamlit/secrets.toml` for local development.
# On Streamlit Community Cloud, set the same key/value in the
# app's "Secrets" panel — do NOT commit the real value to git.

password = "change-me"
