streamlit>=1.32
# Ceiling stops a silent major-version jump. NOT pinned <3: Streamlit Cloud
# runs Python 3.14, for which no pandas 2.x wheel exists — that pin breaks the
# deploy. Local (3.0.3) and prod (3.0.5) are both on 3.0.x; tests cover that.
pandas>=3.0,<4
openpyxl>=3.1
# UI-19 PDF form output (pure-Python wheels; confirmed for Python 3.14)
reportlab>=4.4,<6
pypdf>=6,<7
# Sage report PDF input for the UI-19 form (the standalone script's reader).
# Its deps (pdfminer.six, pypdfium2, Pillow, cryptography) all have Python 3.14
# wheels for Cloud's manylinux x86_64, checked with pip download.
pdfplumber>=0.11,<0.12
